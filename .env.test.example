# Optional overrides for integration tests.
# Root .env is loaded first, then this file overrides test-only values.

TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/station_mgmt_test
LOG_LEVEL=error
